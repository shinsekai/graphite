{
  "name": "@graphite/shared",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Zod schemas and TypeScript types",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "check": "biome check src/**/*.ts",
    "test": "vitest run",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "zod": "^4.0.8"
  },
  "devDependencies": {
    "@graphite/config": "workspace:*",
    "typescript": "^5.8.0",
    "vitest": "^3.0.7"
  }
}
