{
  "name": "@graphite/db",
  "version": "0.1.0",
  "private": true,
  "description": "Drizzle ORM database layer",
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "tsc",
    "check": "biome check src/**/*.ts",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "db:migrate": "bun run src/migrate-runner.ts",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "drizzle-orm": "^0.40.1"
  },
  "peerDependencies": {
    "postgres": "^3.4.0"
  },
  "devDependencies": {
    "@graphite/config": "workspace:*",
    "@types/node": "^22.10.5",
    "drizzle-kit": "^0.31.2",
    "typescript": "^5.8.0",
    "vitest": "^3.0.7"
  }
}
